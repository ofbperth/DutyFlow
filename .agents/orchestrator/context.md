# Context — DutyFlow Group-Scoped Shift Template & Schedule Filtering

## Active Milestones
- **M5: Universal Group-Scoped Shift Template & Schedule Filtering**:
  - Targets: `src/components/SchedulerDashboard.tsx`, `src/firebase.ts`, `src/types.ts`, and all related components (`FourWeekCalendarView.tsx`, `UserDashboard.tsx`, etc.).
  - Central helpers: `getAllowedTargetGroupIdsForHomeGroup`, `CROSS_GROUP_RULES`.
- **M6: Verification & Forensic Audit**:
  - `npm run lint`, `npm test`, `npm run build`.

## State & Dependencies
- Heartbeat cron active (task-15).
- Pending Exploration Phase.
